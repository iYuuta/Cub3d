# run after cloning

git submodule update --init --recursive


# TODO
make makefile go into mlx and make it like libft


delete print_map later

remove relink

make the cube also static to avoid sending it in ft_error
and avoid leaks to destroy images

rename map map 

merge parsing and execution
